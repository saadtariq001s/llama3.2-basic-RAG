streamlit==1.26.0
sqlite3  # Comes pre-installed with Python, no need to install separately
ollama==0.1.0  # Replace with the actual version if available on PyPI
pandas==2.1.1  # For dataframe display
